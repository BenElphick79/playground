# playground

This repo is part of the **GitHub Framework** governed by the
[governance repo](../governance/).

**Purpose**
Safe sandbox for experiments. When something becomes real, promote it
to the right repo.

**Optional setup**
Use `gh` CLI to add labels and create a Projects board (see examples in
[build-scope.md](../governance/build-scope.md)).
